# problem
# Can you change the values inside a list which is contained in set s?
s = {8, 7, 12, "Harry", [1, 2]}

print(s)
# a set can't contain a list cz every element in a set must be immutable and hashable. But lists are mutable and not hashable.
